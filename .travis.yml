language: rust
rust:
  - stable
  - beta
  - nightly
os:
  - linux
  - osx
script:
  - cargo build --verbose --all
  - cargo test --release --all --verbose
matrix:
  allow_failures:
    - rust: nightly
